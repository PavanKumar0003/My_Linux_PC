/*if sale price and profit of n items is given as inputs,  find the cost price of 1 item (Cost price = (Sale price * (100 - Profit%)) / n.*/
#include<stdio.h>
#include <math.h>
int P;
int main()
{
    // int Cost_price,Selling_price,profit,n_items;
    // printf("Enter Selling_price, profit, number of Iteams:");
    // scanf("%d%d%d", &Selling_price,&profit,&n_items);
    // Cost_price = (Selling_price - profit) / n_items;
    // printf("cost price is = %d \n", Cost_price);
    int p = pow(2,2);
    printf("%d",p);
    return 0;
}